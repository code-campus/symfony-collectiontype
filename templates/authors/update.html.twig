{% extends 'base.html.twig' %}

{% set title = "Auteurs (update): "~ author.title %}
{% block title title %}

{% block body %}
<div class="container">

    <div class="page-header">
        <h1>{{ title }}</h1>
    </div>

    <div>
        <a href="{{ path('authors:index') }}">Liste des auteurs</a>
        <a href="{{ path('authors:read', {'id': author.id}) }}">Details de l'auteur</a>
        <a href="{{ path('authors:delete', {'id': author.id}) }}">Supprimer l'auteur</a>
    </div>

    {{ include('authors/form.html.twig') }}
</div>
{% endblock %}
